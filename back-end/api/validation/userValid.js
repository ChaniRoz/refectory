// const Joi = require('joi');

// let userSchema = Joi.object({
//     phone: Joi.string().min(9).max(10),
//     userName: Joi.string().min(2).max(15),
//     email: Joi.string().email(),
//     password: Joi.string()
// })

// exports.validate = (userData) => {
//     return userSchema.validate(userData);
// }